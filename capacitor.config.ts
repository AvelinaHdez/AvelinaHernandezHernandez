import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'avelina-hernandez-hernandez-10a',
  webDir: 'dist'
};

export default config;
